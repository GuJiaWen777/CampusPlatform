server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/platform?serverTimezone=GMT%2B8
    # 数据库用户名
    username: root
    # 数据库密码
    password: gjw523624
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  type-aliases-package: com.ocean.platformspringboot.entity,com.ocean.platformspringboot.entity.dto
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


